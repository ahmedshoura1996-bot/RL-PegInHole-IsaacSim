# M3.2 Investigation — Peg Physics / Collision

## Current Status

M3.2 investigation is ongoing.

## Confirmed Findings

### Peg
- RigidBodyAPI: present
- kinematicEnabled: false
- One rigid body: Object
- COM position matches body frame origin
- Direct PhysX view confirms root pose writes reach PhysX

### Fixture Geometry
- Four fixture bodies are present.
- Fixture collision APIs are present.
- Fixture geometry provides a 21 mm x 21 mm opening.
- Peg diameter = 20 mm.
- Intended clearance = 1 mm diameter.

### Collision Tests
- Fixture collisions disabled: Peg still converges to Z ≈ 0.022 m.
- Robot collision audit: 0 robot collision prims detected.
- Table normal stage audit: no CollisionAPI detected under Table.
- Ground plane is at Z = -1.05 m and is not a plausible cause.
- Manual PhysX placement to Z = 0.010 m is accepted initially.
- After simulation, Peg returns to Z ≈ 0.022 m.

### PhysX Observation

Direct PhysX test:

Z = 0.010 m after write_root_state_to_sim.

After simulation:

Z ≈ 0.02197 m on the first step.

The Peg then converges to:

Z ≈ 0.02200 m.

This confirms that the correction occurs at the PhysX simulation level.

## USD Prototype Investigation

The stage contains 24 USD prototypes.

Several prototypes contain collision meshes.

Direct modification of prototype collision properties is not allowed because USD instancing prototypes are read-only for authoring.

Example error:

"authoring to an instancing prototype is not allowed."

Therefore prototype collision properties should not be modified directly.

## Current Hypothesis

The source of the Z ≈ 0.022 m correction has not yet been conclusively identified.

The next investigation should identify the relationship between scene instances and USD prototypes, or directly identify the PhysX contact/constraint source.

## Important Rule

Do not modify the main environment configuration until the source of the Z ≈ 0.022 m correction is identified.

## Git Checkpoint

Last confirmed project checkpoint:

30e5363
M3.2 checkpoint: validate fixture transforms and collisions
